.pragma library

function i(text) {
    return "<i>" + text + "</i>"
}
function b(text) {
    return "<b>" + text + "</b>"
}
function u(text) {
    return "<u>" + text + "</u>"
}
function s(text) {
    return "<s>" + text + "</s>"
}

